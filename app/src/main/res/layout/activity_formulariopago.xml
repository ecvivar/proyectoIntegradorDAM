<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/verde_fondo_app"
    android:padding="16dp"
    tools:context=".FormularioPagoActivity">

    <!-- Logo  -->
    <ImageView
        android:id="@+id/iv_logo"
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:contentDescription="Logo Freekoders"
        android:src="@drawable/logoclub"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Botón back -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnVolverFormulario"
        style="@style/Widget.Material3.Button.ElevatedButton.Icon"
        android:layout_width="0dp"
        android:layout_height="60dp"
        android:layout_marginTop="16dp"
        android:text="DATOS DEL PAGO"
        android:textColor="@color/white"
        android:textSize="18sp"
        app:backgroundTint="@color/verde_boton_app"
        app:shapeAppearance="@style/ShapeAppearance.App.Button.EsquinasPersonalizadas"
        app:icon="@drawable/arrow_back"
        app:iconTint="@color/white"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/iv_logo" />

    <!-- Bloque Monto a pagar  -->
    <LinearLayout
        android:id="@+id/layout_monto"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="24dp"
        android:background="@drawable/bg_rounded_corners_top"
        android:orientation="vertical"
        android:padding="12dp"
        app:layout_constraintTop_toBottomOf="@id/btnVolverFormulario"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <TextView
            android:id="@+id/monto"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Monto a pagar"
            android:textColor="@android:color/black"
            android:textSize="16sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/valorprecio"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="$10.000"
            android:textColor="@android:color/black"
            android:textSize="14sp" />
    </LinearLayout>

    <!-- Bloque Formas de pago  -->
    <LinearLayout
        android:id="@+id/layout_formas_pago"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
    android:background="@color/white"
    android:orientation="vertical"
    android:padding="12dp"
    app:layout_constraintTop_toBottomOf="@id/layout_monto"
    app:layout_constraintStart_toStartOf="parent"
    app:layout_constraintEnd_toEndOf="parent">

    <TextView
        android:id="@+id/formaspago"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Formas de pago"
        android:textColor="@android:color/black"
        android:textSize="16sp"
        android:textStyle="bold" />
    <RadioGroup
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">

        <RadioButton
            android:id="@+id/rb_efectivo"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Efectivo"
            android:textSize="14sp"
            android:minHeight="48dp"
            android:checked="true"
            android:buttonTint="@color/black"/>
        <RadioButton
            android:id="@+id/rb_tarjeta_credito"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Tarjeta de crédito"
            android:textSize="14sp"
            android:minHeight="48dp"
            android:buttonTint="@color/black"/>
    </RadioGroup>
</LinearLayout>

    <!-- Bloque Cantidad de cuotas  -->
    <LinearLayout
    android:id="@+id/layout_cuotas"
    android:layout_width="0dp"
    android:layout_height="wrap_content"
    android:layout_marginTop="12dp"
    android:background="@drawable/bg_rounded_corners_bottom"
    android:orientation="vertical"
    android:padding="12dp"
    app:layout_constraintTop_toBottomOf="@id/layout_formas_pago"
    app:layout_constraintStart_toStartOf="parent"
    app:layout_constraintEnd_toEndOf="parent">

<TextView
android:id="@+id/tv_cuotas_label"
android:layout_width="match_parent"
android:layout_height="wrap_content"
android:layout_marginBottom="4dp"
android:text="Cantidad de cuotas"
android:textColor="@android:color/black"
android:textSize="16sp"
android:textStyle="bold" />

<Spinner
android:id="@+id/sp_cuotas"
android:layout_width="match_parent"
android:layout_height="wrap_content"
android:minHeight="48dp"
android:layout_marginTop="4dp"
android:paddingStart="8dp"
android:paddingEnd="8dp"
android:background="#FFEEEEEE"
android:entries="@array/cuotas_options"
android:spinnerMode="dropdown"
android:contentDescription="Selector de cuotas"
android:popupBackground="@android:color/white"
android:dropDownSelector="@android:color/darker_gray" />
    </LinearLayout>

    <!-- Botón Pagar -->
<com.google.android.material.button.MaterialButton
android:id="@+id/btn_pagar"
android:layout_width="wrap_content"
android:layout_height="wrap_content"
android:text="Pagar"
android:textColor="@color/white"
android:backgroundTint="@color/verde_boton_app"
android:paddingStart="32dp"
android:paddingEnd="32dp"
android:paddingTop="16dp"
android:paddingBottom="16dp"
android:textSize="16sp"
android:textStyle="bold"
app:layout_constraintBottom_toBottomOf="parent"
app:layout_constraintStart_toStartOf="parent"
app:layout_constraintEnd_toEndOf="parent"
app:layout_constraintTop_toBottomOf="@id/layout_cuotas"
app:layout_constraintVertical_bias="0.8"/>

    </androidx.constraintlayout.widget.ConstraintLayout>
